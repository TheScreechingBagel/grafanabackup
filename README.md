## A python script to back up all dashboards from Grafana, uploading them to aws s3 under a directory with current UTC date and hour, with kubernetes cronjob setup

### Run:
Environment variables are set 

tbd